import { useGetProductsQuery } from '../../hooks/productHooks'
import { ProductPageSection } from './index.styled'

const ProductPage = () => {
  // const { data: products, isLoading, error } = useGetProductsQuery()
  // return isLoading ? (
  //   'loading'
  // ) : error ? (
  //   'error'
  // ) : (
  //   <ProductPageSection>
  //     {products!.map((el) => {
  //       return <li>{el.price}</li>
  //     })}
  //   </ProductPageSection>
  // )
}

export default ProductPage
